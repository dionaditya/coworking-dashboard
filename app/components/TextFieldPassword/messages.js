/*
 * TextFieldPassword Messages
 *
 * This contains all the text for the TextFieldPassword component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TextFieldPassword';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TextFieldPassword component!',
  },
});
