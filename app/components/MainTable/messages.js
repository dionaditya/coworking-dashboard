/*
 * MainTable Messages
 *
 * This contains all the text for the MainTable component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MainTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MainTable component!',
  },
});
